server:
  port: 8888

spring:
  application:
    name: config-server
#  rabbitmq:
#    host: "${HOST_IP}"
#    port: "${RABBIT_MQ_PORT}"
#    username: "${RABBIT_MQ_USERNAME}"
#    password: "${RABBIT_MQ_PASSWORD}"
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.gabia.com/mentoring/newbie/2021.05/mentoring-alarm/playground/yml-config-files.git
          username: gm2202987
          password: "!lattack1!@"

management:
  endpoints:
    web:
      exposure:
        include: health, busrefresh